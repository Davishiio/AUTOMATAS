Entrega del proyecto, contiene el g4, la clase que hereda del listener (InTraductorTostr) y la clase principal.py la cual es la que ejecuta
pero antes debe generar el grammar.
el archivo que ingresa es "TXT.txt" y para que le exporte un ejecutable en python hace lo siguiente:
*NombreDeLaClase*.py
ejemplo:
Resultado.py
